<Window x:Class="WPFUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Note Manager" Height="700" Width="800">
    <TabControl>
        <!-- Create Note Tab -->
        <TabItem Header="Create Note">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Enter note text:"/>
                <TextBox Text="{Binding CreateNoteText, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                <Button Content="Create Note" Command="{Binding CreateNoteCommand}" 
                        Height="30" Margin="0,10,0,0"/>
            </Grid>
        </TabItem>

        <!-- View Note Tab -->
        <TabItem Header="View Note">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Note ID:"/>
                <TextBox Text="{Binding ViewNoteId, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
                <TextBox Text="{Binding ViewNoteResult}" IsReadOnly="True" 
                         AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0"/>
                <Button Content="Get Note" Command="{Binding GetNoteCommand}" 
                        Height="30" Margin="0,10,0,0"/>
            </Grid>
        </TabItem>

        <!-- Update Note Tab -->
        <TabItem Header="Update Note">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Note ID:"/>
                <TextBox Text="{Binding UpdateNoteId, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
                <Label Content="New text:" Margin="0,10,0,0"/>
                <TextBox Text="{Binding UpdateNoteText, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,5,0,0"/>
                <Button Content="Update Note" Command="{Binding UpdateNoteCommand}" 
                        Height="30" Margin="0,10,0,0"/>
            </Grid>
        </TabItem>

        <!-- Delete Note Tab -->
        <TabItem Header="Delete Note">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Note ID:"/>
                <TextBox Text="{Binding DeleteNoteId, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
                <Button Content="Delete Note" Command="{Binding DeleteNoteCommand}" 
                        Height="30" Margin="0,10,0,0"/>
            </Grid>
        </TabItem>
    </TabControl>
</Window>